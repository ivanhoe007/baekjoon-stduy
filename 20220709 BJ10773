#include <stdio.h>

int main()
{
    printf("Hello world!");
}








/*
#include <stdio.h>


int cnt = 0;
int stack[100001];

void push(int n) {
    stack[cnt] = n;
    cnt++;
}

void pop() {
    cnt--;
    stack[cnt] = 0;
}

int main()
{
    int K;
    int sum = 0;
    scanf("%d", &K);
    int data[K];
    for (int i = 0; i < K; i++) 
	{
        scanf("%d", &data[i]);
        if (data[i] == 0) 
		{
            pop();
        }
        else 
		{
            push(data[i]);
        }
    }
    
    for (int i = 0; i < cnt; i++) 
	{
        sum += stack[i];
    }
    
    printf("%d\n", sum);
    return 0;
}
/*